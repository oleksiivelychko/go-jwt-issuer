apiVersion: v1
kind: Service
metadata:
  name: gojwtissuer
  namespace: gojwtissuer
  labels:
    app: gojwtissuer
spec:
  type: LoadBalancer
  selector:
    app: gojwtissuer
  ports:
    - port: 8080
      targetPort: 8080
      name: http
